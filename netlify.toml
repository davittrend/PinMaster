[build]
functions = "netlify/functions"
publish = "dist"  # Adjust this if your static assets are published elsewhere (e.g., in `public` or `build`)

[[redirects]]
from = "/callback"
to = "/.netlify/functions/pinterest-callback"
status = 200
